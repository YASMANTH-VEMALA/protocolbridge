NODE_ENV=development
PORT=4000
DATABASE_URL=postgresql://protocolbridge:protocolbridge@localhost:55432/protocolbridge?schema=public
JWT_SECRET=replace-with-at-least-32-random-characters
AGENT_CREDENTIAL_PEPPER=replace-with-at-least-32-random-characters
APPROVAL_LINK_SECRET=replace-with-a-different-32-character-secret
BUYER_APPROVAL_BASE_URL=http://localhost:3000/approve
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:3002

# Optional live intent resolver. Configure both or neither.
OPENAI_API_KEY=
OPENAI_MODEL=

# Optional at startup, required for real Test Mode checkout.
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
RAZORPAY_WEBHOOK_SECRET=

NEXT_PUBLIC_API_URL=http://localhost:4000
API_URL=http://localhost:4000
ENABLE_GOLDEN_DEMO=true
DEMO_AGENT_API_KEY=pb_test_solekart_agent_2026
DEMO_BUYER_EMAIL=buyer@protocolbridge.local
DEMO_BUYER_PASSWORD=DemoPass!2026

# Optional seed overrides. Keep these aligned with the demo proxy values above.
# SEED_AGENT_API_KEY=replace-with-a-non-production-agent-key
# SEED_DEMO_PASSWORD=replace-with-a-non-production-demo-password
